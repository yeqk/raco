import 'package:flutter/material.dart';

class MissingRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Missing Route'),
      ),
    );
  }
}